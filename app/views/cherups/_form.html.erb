<%= form_for(@cherup) do |f| %>
  <% if @cherup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cherup.errors.count, "error") %> prohibited this cherup from being saved:</h2>

      <ul>
      <% @cherup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :img %><br />
    <%= f.text_field :img %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
